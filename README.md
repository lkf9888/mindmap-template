# 思维脑图绘制模板

这是一个思维脑图绘制模板，包含节点创建、删除、拖拽、尺寸调整、文字样式编辑、连线编辑、连接线文字注释、节点备注、撤回、画布缩放和平移、多脑图目录、云同步，以及只读分享链接。

## 打开方式

本项目包含 Vercel API 云同步接口。只看静态界面可以运行 `npm run dev`；需要完整测试登录和云同步时请使用：

```bash
npm run dev:vercel
```

然后访问 `http://localhost:5174`。

## 当前功能

- 顶部提供撤回按钮，可恢复最近的节点、文字、连线、备注、尺寸和排布编辑。
- 右键画布新建节点，右键节点删除、复制、添加备注或创建子节点。
- 节点支持标题、文字段落、超链接，以及颜色、形状、文字大小、字体和拖拽尺寸修改。
- 节点备注使用长文本编辑框录入，节点右上角显示备注按钮，展开后的备注框可关闭并拖拽改变大小。
- 超链接节点会显示“访问 Link”按钮，可直接打开链接。
- 节点颜色提供 24 个快速色块，也保留自定义颜色输入。
- 右键节点支持复制本节点，节点可拖到任意方向，不限制在固定画布边界内。
- 新建连接线默认直线，连接线支持颜色、实线/虚线、直线/曲线/折线、箭头方向和文字注释。
- 鼠标滚轮缩放，拖动画布平移，工具栏可横向/纵向整理节点。
- 左侧目录支持创建多个脑图、切换脑图、重命名当前脑图。
- 登录后数据会先保存在浏览器本地，并同步到 Vercel Blob 私有云存储；换设备登录同一账号会加载云端脑图。
- 每次覆盖云端数据前会保留云端备份，本地仍保留浏览器安全备份。
- 顶部语言按钮支持简体中文/英文界面切换。
- 点击“分享只读链接”会复制一个带脑图快照的 URL，别人打开后会直接进入纯画布只读视图，看不到顶部、左侧和右侧编辑模块。
