{
  "name": "mindmap-template",
  "version": "1.0.0",
  "private": true,
  "description": "A simple static mind map drawing website template.",
  "scripts": {
    "dev": "python3 -m http.server ${PORT:-5174}",
    "dev:vercel": "npx vercel dev --listen 5174",
    "check": "node --check app.js && node --check api/auth.js && node --check api/maps.js"
  },
  "dependencies": {
    "@vercel/blob": "^2.4.0"
  }
}
